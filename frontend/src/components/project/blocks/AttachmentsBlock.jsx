import React from 'react';
import {
    Stack,
    Typography,
} from '@mui/material';

export default function AttachmentsBlock({

}) {
    return (
        <Stack
            direction="column"
        >
            <Typography variant="h6">Прикреплённый файлы</Typography>
        </Stack>
    )
}