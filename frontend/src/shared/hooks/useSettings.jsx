import { useContext } from 'react';

import { useSettingsContext } from './../../widgets/theme-settings';

// ----------------------------------------------------------------------

const useSettings = useSettingsContext

export default useSettings;
